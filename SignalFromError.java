public class SignalFromError 
{
  Process process=new Process();//creer un objet de type Processes qui va 
                              //retourner la methode calculateResponse (c.a.d chaque Y(t) vs. t
  process.calculateResponse(disturbance=To, step change in q, t);////peut importe pour le moment
  public double [] error(process);
  {
   for (int i=0; i<process.lenght; i++)
    return step change-process[i];
  }

  ControllerEssay controller=new ControllerEssay();
  controller.calculateSignal(double error);
}